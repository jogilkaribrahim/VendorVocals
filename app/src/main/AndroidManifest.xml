<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vendorvocals">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VendorVocals">
        <activity android:name=".AdminAddService"></activity>
        <activity android:name=".ReportActivity" />
        <activity android:name=".VendorVerification" />
        <activity android:name=".AdminMenuActivity" />
        <activity android:name=".AdminPanelActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".VerifyOTP" />
        <activity android:name=".SendOTP" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>